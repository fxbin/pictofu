# Release evidence boundary

Repository evidence can prove code quality gates but cannot by itself prove:

- a Vercel production project exists;
- `pictofu.com` points to the intended deployment;
- camera behavior works on physical devices;
- mobile download/share works in the target browsers;
- Search Console accepted the sitemap.

Those items remain control/data-plane evidence and keep the semantic release verdict at `HOLD` until verified.
